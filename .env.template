# AWS Configuration
AWS_REGION=us-east-1  # Replace with your preferred region
AWS_PROFILE=default   # Replace with your AWS profile if not using default

# Application Configuration
ENVIRONMENT=dev       # dev or prod
BUCKET_NAME=thisisacatforsureyouknowit-dev  # Set by SAM deployment

# Optional: Content Moderation Settings
MODERATION_CONFIDENCE_THRESHOLD=50.0
MAX_FILE_SIZE=1048576  # 1MB in bytes